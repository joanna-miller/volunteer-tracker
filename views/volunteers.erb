<h1>Volunteers</h1>

<% if @volunteers.any? %>
  <ul>
    <% @volunteers.each do |volunteer| %>
      <li><a href="/projects/<%= volunteer.project_id %>/volunteers/<%= volunteer.id %>"><%= volunteer.name %></a></li>
    <% end %>
  </ul>
  
<% else %>
  <p>There are currently no volunteers signed up.</p>
<% end %>

